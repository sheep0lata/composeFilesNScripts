version: "3"
services:
        swagify:
                image: "swagify:stable"
                container_name: "swagify"
                environment:
                        - VIRTUAL_HOST=sg.sheepo.fr
                        - LETSENCRYPT_HOST=sg.sheepo.fr
                networks:
                        - net2
                restart: "always"

networks:
        net2:
                external: true
